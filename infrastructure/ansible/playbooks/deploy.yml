---
- name: Déploiement Locatic sur minikube
  hosts: localhost
  gather_facts: false

  vars:
   project_root: "/mnt/c/Users/HP.COM/Desktop/Mini-projet-DevOps-WEB2-feature-initial-devops-setup"

   k8s_manifests_dir: "{{ project_root }}/k8s"
   terraform_dir: "{{ project_root }}/infrastructure/terraform"

   namespace: "locatic"
   docker_image: "ghcr.io/owner/locatic:latest"
   app_replicas: "1"
   pvc_name: "locatic-sqlite-pvc"

   minikube_cmd: minikube.exe
   kubectl_cmd: kubectl
   terraform_cmd: /mnt/c/Tools/Terraform/terraform.exe

  tasks:
    - name: Vérifier que Minikube est disponible
      ansible.builtin.command: "{{ minikube_cmd }} status"
      register: minikube_status
      changed_when: false
      failed_when: minikube_status.rc != 0

    - name: Vérifier que kubectl est disponible
      ansible.builtin.command: "{{ kubectl_cmd }} version --client"
      changed_when: false

    - name: Vérifier que Terraform est disponible
      ansible.builtin.command: "{{ terraform_cmd }} version"
      changed_when: false

    - name: Lire les outputs Terraform
      ansible.builtin.command: "{{ terraform_cmd }} output -json"
      args:
        chdir: "{{ terraform_dir }}"
      register: terraform_outputs
      changed_when: false

    - name: Charger les variables Terraform
      ansible.builtin.set_fact:
        tf: "{{ terraform_outputs.stdout | from_json }}"

    - name: Appliquer les manifests Kubernetes
      ansible.builtin.command: >
        {{ kubectl_cmd }} apply
        -f {{ k8s_manifests_dir }}/{{ item }}
        --namespace {{ tf.namespace.value | default(namespace) }}
      loop:
        - app/configmap.yaml
        - app/deployment.yaml
        - app/service.yaml
        - nginx/configmap.yaml
        - nginx/deployment.yaml
        - nginx/service.yaml
        - monitoring/prometheus-configmap.yaml
        - monitoring/prometheus-deployment.yaml
        - monitoring/prometheus-service.yaml
        - monitoring/grafana-configmap.yaml
        - monitoring/grafana-deployment.yaml
        - monitoring/grafana-service.yaml
      environment:
        LOCATIC_NAMESPACE: "{{ tf.namespace.value | default(namespace) }}"
        LOCATIC_IMAGE: "{{ tf.docker_image.value | default(docker_image) }}"
        LOCATIC_REPLICAS: "{{ tf.app_replicas.value | default(app_replicas) | string }}"
        LOCATIC_PVC: "{{ tf.pvc_name.value | default(pvc_name) }}"

    - name: Mettre à jour l'image de l'application
      ansible.builtin.command: >
        {{ kubectl_cmd }} set image deployment/locatic-app
        locatic={{ tf.docker_image.value | default(docker_image) }}
        -n {{ tf.namespace.value | default(namespace) }}
      register: set_image
      changed_when: "'configured' in set_image.stdout or 'updated' in set_image.stdout"

    - name: Attendre le déploiement de l'application
      ansible.builtin.command: >
        {{ kubectl_cmd }} rollout status deployment/locatic-app
        -n {{ tf.namespace.value | default(namespace) }}
        --timeout=180s
      changed_when: false

    - name: Attendre le déploiement de Nginx
      ansible.builtin.command: >
        {{ kubectl_cmd }} rollout status deployment/locatic-nginx
        -n {{ tf.namespace.value | default(namespace) }}
        --timeout=120s
      changed_when: false

    - name: Afficher l'URL du service
      ansible.builtin.command: >
        {{ minikube_cmd }} service locatic-nginx
        -n {{ tf.namespace.value | default(namespace) }}
        --url
      register: service_url
      changed_when: false

    - name: Résumé du déploiement
      ansible.builtin.debug:
        msg:
          - "Namespace : {{ tf.namespace.value | default(namespace) }}"
          - "Image : {{ tf.docker_image.value | default(docker_image) }}"
          - "URL Nginx : {{ service_url.stdout_lines | join(', ') }}"