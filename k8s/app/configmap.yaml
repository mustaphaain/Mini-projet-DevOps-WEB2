apiVersion: v1
kind: ConfigMap
metadata:
  name: locatic-app-config
  labels:
    app: locatic
data:
  ASPNETCORE_URLS: "http://+:8080"
  DisableHttpsRedirect: "true"
  ConnectionStrings__DefaultConnection: "Data Source=/data/locatic.db"
