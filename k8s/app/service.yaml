apiVersion: v1
kind: Service
metadata:
  name: locatic-app
  labels:
    app: locatic
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: locatic
    tier: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
