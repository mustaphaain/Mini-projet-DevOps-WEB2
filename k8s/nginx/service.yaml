apiVersion: v1
kind: Service
metadata:
  name: locatic-nginx
  labels:
    app: locatic-nginx
    tier: proxy
spec:
  type: NodePort
  selector:
    app: locatic-nginx
    tier: proxy
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
